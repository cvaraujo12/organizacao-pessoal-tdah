{
  "name": "organizador-pessoal-tdah",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "sistema/painel"
  ],
  "scripts": {
    "painel:dev": "npm run dev --workspace=sistema/painel",
    "painel:build": "npm run build --workspace=sistema/painel",
    "painel:start": "npm run start --workspace=sistema/painel",
    "painel:test": "npm run test --workspace=sistema/painel",
    "setup": "npm install && npm run painel:build",
    "clean": "rm -rf node_modules && rm -rf sistema/painel/node_modules"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "cross-env": "^7.0.0"
  }
} 